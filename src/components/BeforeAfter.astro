---
/**
 * Before/After comparison showing the value of SIP privacy
 */
---

<section class="before-after-section">
  <h2 class="section-title">Why Privacy Matters</h2>
  <p class="section-subtitle">Every transaction tells a story. Control who reads yours.</p>

  <div class="comparison-grid">
    <div class="comparison-card before">
      <div class="card-header">
        <span class="card-icon">👁</span>
        <span class="card-label">WITHOUT SIP</span>
      </div>
      <div class="card-content">
        <div class="tx-row">
          <span class="tx-label">From:</span>
          <span class="tx-value visible">0x1234...abcd</span>
        </div>
        <div class="tx-row">
          <span class="tx-label">To:</span>
          <span class="tx-value visible">0x5678...efgh</span>
        </div>
        <div class="tx-row">
          <span class="tx-label">Amount:</span>
          <span class="tx-value visible">1,000 USDC</span>
        </div>
      </div>
      <div class="card-footer warning">
        Everyone sees everything
      </div>
    </div>

    <div class="comparison-arrow">→</div>

    <div class="comparison-card after">
      <div class="card-header">
        <span class="card-icon">🛡</span>
        <span class="card-label">WITH SIP</span>
      </div>
      <div class="card-content">
        <div class="tx-row">
          <span class="tx-label">From:</span>
          <span class="tx-value hidden">████████████</span>
        </div>
        <div class="tx-row">
          <span class="tx-label">To:</span>
          <span class="tx-value hidden">████████████</span>
        </div>
        <div class="tx-row">
          <span class="tx-label">Amount:</span>
          <span class="tx-value hidden">████████████</span>
        </div>
      </div>
      <div class="card-footer success">
        Only you control access
      </div>
    </div>
  </div>
</section>

<style>
  .before-after-section {
    padding: 4rem 1.5rem;
    text-align: center;
  }

  .section-title {
    font-size: 1.75rem;
    font-weight: 700;
    margin: 0 0 0.5rem;
    color: var(--sl-color-white);
  }

  .section-subtitle {
    font-size: 1.1rem;
    color: var(--sl-color-gray-3);
    margin: 0 0 2.5rem;
  }

  .comparison-grid {
    display: flex;
    align-items: stretch;
    justify-content: center;
    gap: 2rem;
    max-width: 900px;
    margin: 0 auto;
  }

  .comparison-card {
    flex: 1;
    max-width: 320px;
    display: flex;
    flex-direction: column;
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 1rem;
    overflow: hidden;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }

  .comparison-card:hover {
    transform: translateY(-4px);
  }

  .comparison-card.before:hover {
    box-shadow: 0 8px 30px rgba(239, 68, 68, 0.15);
  }

  .comparison-card.after:hover {
    box-shadow: 0 8px 30px rgba(139, 92, 246, 0.25);
  }

  .card-header {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.75rem;
    padding: 1rem;
    background: rgba(255, 255, 255, 0.02);
    border-bottom: 1px solid rgba(255, 255, 255, 0.05);
  }

  .card-icon {
    font-size: 1.25rem;
  }

  .card-label {
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--sl-color-gray-3);
  }

  .card-content {
    padding: 1.5rem;
    flex: 1;
  }

  .tx-row {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0.625rem 0;
    border-bottom: 1px solid rgba(255, 255, 255, 0.05);
  }

  .tx-row:last-child {
    border-bottom: none;
  }

  .tx-label {
    font-size: 0.875rem;
    color: var(--sl-color-gray-3);
  }

  .tx-value {
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, monospace;
    font-size: 0.875rem;
  }

  .tx-value.visible {
    color: #f87171;
  }

  .tx-value.hidden {
    color: #8b5cf6;
    letter-spacing: 0.05em;
  }

  .card-footer {
    padding: 0.75rem 1rem;
    font-size: 0.8rem;
    font-weight: 500;
  }

  .card-footer.warning {
    background: rgba(239, 68, 68, 0.1);
    color: #f87171;
    border-top: 1px solid rgba(239, 68, 68, 0.2);
  }

  .card-footer.success {
    background: rgba(139, 92, 246, 0.1);
    color: #a78bfa;
    border-top: 1px solid rgba(139, 92, 246, 0.2);
  }

  .comparison-arrow {
    font-size: 2rem;
    color: var(--sl-color-gray-4);
    flex-shrink: 0;
    align-self: center;
  }

  /* Light theme */
  :root[data-theme='light'] .comparison-card {
    background: rgba(0, 0, 0, 0.02);
    border-color: rgba(0, 0, 0, 0.08);
  }

  :root[data-theme='light'] .section-title {
    color: var(--sl-color-black);
  }

  :root[data-theme='light'] .tx-value.visible {
    color: #dc2626;
  }

  :root[data-theme='light'] .tx-value.hidden {
    color: #7c3aed;
  }

  :root[data-theme='light'] .card-footer.warning {
    background: rgba(220, 38, 38, 0.05);
    color: #dc2626;
  }

  :root[data-theme='light'] .card-footer.success {
    background: rgba(124, 58, 237, 0.05);
    color: #7c3aed;
  }

  @media (max-width: 768px) {
    .comparison-grid {
      flex-direction: column;
      gap: 1rem;
    }

    .comparison-card {
      max-width: 100%;
      width: 100%;
    }

    .comparison-arrow {
      transform: rotate(90deg);
    }
  }
</style>
